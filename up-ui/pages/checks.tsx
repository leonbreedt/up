import { NextPage } from 'next';
import CheckList from 'components/check-list';

const Checks: NextPage = () => {
  return (
    <main>
      <CheckList />
    </main>
  );
};

export default Checks;
